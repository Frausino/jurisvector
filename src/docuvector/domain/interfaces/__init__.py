"""Interfaces (contratos) que a camada de aplicação consome.

Implementações concretas vivem em `infrastructure/`. Aqui temos apenas
abstrações puras (Protocol) que permitem Dependency Inversion (D do SOLID).
"""
